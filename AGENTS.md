# Codex 模块治理产品规则

## 产品职责

- Boss + 唯一获授权总设计师负责整个项目施工大纲、总体架构、模块归属、优先级和跨模块协调。总设计师可以是 Fable 5、`construction-outline-planner` 所在窗口或 Boss 指定的其他规划方。
- 同一大纲版本不得由两个总设计师同时续编；切换总设计师必须由 Boss 指定并保留交接和旧版。
- Codex 模块中央只负责 Codex 模块内部的任务、窗口、证据、冲突与下一步建议。
- 中央处理器不得直接施工业务链路，不得另建全系统详细任务计划。
- 总设计师窗口只维护全项目大纲和 `NOT_REGISTERED` 候选任务，不写工程总账、不占用对象、不派工；Codex 中央只登记明确归属 Codex 的范围。
- 中央首次接收冻结大纲时必须一次形成全部 Codex 任务、Boss 决策、依赖链和并行波次，不得逐任务重复向 Boss 索要同一范围批准。
- Boss 对唯一中央启动图的明确批准可覆盖图中列明的任务和后续波次；范围或风险变化时受影响批准立即失效。

## 默认模型

- Codex 模块中央：`gpt-5.6-sol`。
- Codex 规划窗口：`gpt-5.6-terra`。
- 新任务窗口与一级子 Agent：`gpt-5.6-terra`。
- 任何例外必须在任务包中由 Boss 明确指定。

## 安全边界

- 不提交真实业务数据、用户总账、证据、TEST 数据、密钥、Cookie 或本机绝对路径。
- 同一对象同一时间只允许一个写入方；未知写入者、范围重叠或权限不清时硬停。
- 当前没有经验证的原生对象级写前拦截；规则文本和提示词不能被称为硬拦。
- 父任务窗口最多派发 3 个一级子 Agent；子 Agent 不得派发孙 Agent。
- 新任务必须绑定 Codex 保存项目；Git 项目默认使用标准 worktree。禁止用自定义任务目录或 `projectless` 冒充同项目派发。
- 标准 worktree 创建后若项目 ID 为空，只允许对同一任务执行“项目本地→原 worktree”往返交接修复；最终回读仍不一致时硬停，不新建平行任务。

## 状态与验收

- 施工窗口只能回传 `NEEDS_REVIEW`、`PARTIAL`、`CONFLICT` 或 `UNKNOWN`。
- 只有中央独立验收并经 Boss 确认后，任务才可写为 `DONE`。
- 本仓库的产品自检通过，不等于业务验收通过。
