# Codex 模块治理产品规则

## 产品职责

- Boss + 唯一获授权总设计师负责整个项目施工大纲、总体架构、模块归属、优先级和跨模块协调。总设计师可以是 Fable 5、`construction-outline-planner` 所在窗口或 Boss 指定的其他规划方。
- 同一大纲版本不得由两个总设计师同时续编；切换总设计师必须由 Boss 指定并保留交接和旧版。
- Codex 模块中央只负责 Codex 模块内部的任务、窗口、证据、冲突与下一步建议。
- 中央处理器不得直接施工业务链路，不得另建全系统详细任务计划。
- 总设计师窗口只维护全项目大纲和 `NOT_REGISTERED` 候选任务，不写工程总账、不占用对象、不派工；Codex 中央只登记明确归属 Codex 的范围。
- 中央首次接收冻结大纲时集中说明 Codex 本期成果、近期任务、必要依赖及 Boss 决策；远期保持轮廓，不强制提前细拆全部任务或增加波次，不得逐任务重复索要同一范围批准。
- Boss 对唯一中央启动图的明确批准可覆盖图中列明的任务和后续波次；范围或风险变化时受影响批准立即失效。
- 已授权范围内的执行排序与未开工任务纯排程等待，可以通过 C09 `revise-plan` 改版并保留原批准，规则归口中央启动图协议第 8 节。不得把业务必要依赖、任务拆合或运行中纠偏伪装成纯排程调整。

## 默认模型

- Codex 模块中央沿用 Boss 当前选择，可自由换模型；模型不构成角色身份或重复授权条件。
- Codex 规划窗口保留 Boss 当前模型；获授权新建且未指定时默认 `gpt-5.6-terra`。
- 新任务窗口与一级子 Agent 默认 `gpt-5.6-terra`，允许 Boss 明确选择其他平台支持模型；复用和续办没有切换要求时保留当前模型，不强制覆盖。
- 模型必须有真实运行读回，程序与规则共同归口 `plugins/codex-module-governance/references/runtime-model-policy.md`。非 Terra 本身不是拒绝理由；身份、项目、权限、占用与独立验收仍分别核验。
- 新任务窗口默认使用原生完全访问权限；仅 Boss 主动选择时使用 Codex 原生 `:workspace` 高隔离模式。系统不得创建、安装或依赖自定义权限 Profile。无论选择哪种模式，都必须留下与真实运行身份关联的权限回执，不能只靠文字承诺。

## 安全边界

- 不提交真实业务数据、用户总账、证据、TEST 数据、密钥、Cookie 或本机绝对路径。
- 同一对象同一时间只允许一个写入方；未知写入者、范围重叠或权限不清时硬停。
- 当前没有经验证的原生对象级写前拦截；规则文本和提示词不能被称为硬拦。
- 系统完全访问不等于业务无限授权；任务仍只能在批准的任务包、对象占用和业务范围内施工。删除、批量覆盖、付款、正式上线、外发和重大权限变更仍须 Boss 单独批准。
- 父任务窗口最多派发 3 个一级子 Agent；子 Agent 不得派发孙 Agent。
- 新任务必须绑定 Codex 保存项目；Git 项目默认使用标准 worktree。禁止用自定义任务目录或 `projectless` 冒充同项目派发。
- 标准 worktree 创建后项目 ID 缺失不等于项目错误。按 C10 项目归属协议核对真实创建关联、原生任务读回和保存项目/Git 目录证据；保留直接读回缺失标记，矛盾或证据不足才停。不因缺字段强制本地往返或新建平行任务，旧封存派发单仍按原合同核验。
- 原生任务标题、项目分组、置顶、归档和“最近”只是界面投影，不是 C03 状态。只有 C06 + Boss 已 `DONE` 且窗口 `RETIRED` 才允许归档；当前中央/裁定可置顶，任务窗口不置顶。

## 状态与验收

- 施工窗口只能回传 `NEEDS_REVIEW`、`PARTIAL`、`CONFLICT` 或 `UNKNOWN`。
- 只有中央独立验收并经 Boss 确认后，任务才可写为 `DONE`。
- 本仓库的产品自检通过，不等于业务验收通过。
