interface:
  display_name: "回传与独立验收器"
  short_description: "凭回传票据隔离验收，并保留 Boss 最终 DONE 门槛"
  default_prompt: "使用 $independent-handover-validator 对已取得 TASK_EVENT_QUEUED 回执的回传票据独立验收；中央只收结论和回执，Boss 最终批准后才可写入 DONE 并自动续派。"
