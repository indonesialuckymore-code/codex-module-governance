# C07 施工回执：裁定请求整理器

状态：`DONE`（2026-08-12，已完成独立隔离验收）。C07 已实现为 `adjudication-request-organizer`；只处理虚构治理数据，不代表已对任何真实业务问题作出裁定。

## 本次新增

- C07 Skill、UI 元数据、确定性整理脚本和四份数据契约。
- 裁定请求、顾问意见、Boss 决定和原窗口回传包的不可覆盖记录。
- C03 `adjudications` 分区复用及实际总账回执绑定。
- 顾问只回 Boss、Boss 决定才回原窗口的路由门禁。
- 项目唯一 `CURRENT_ADJUDICATION` 路由；首次继承中央上下文，后续只追加裁定材料。
- Boss 决定后另生成不含讨论全文的中央影响摘要。
- 12 项 C07 隔离回归；C01–C07 全链共 60 项测试。

## 验收结论

| 验收点 | 结果 |
|---|---|
| 不建第二套状态 | C07 复用 C03，原任务状态全过程不变。 |
| 顾问不替 Boss | 顾问只能推荐、要补证或无安全建议；不能生成窗口回传包。 |
| Boss 决定回原窗口 | 回传包绑定原任务和原窗口，不能选择请求外方案。 |
| 不自动施工 | 请求、建议、决定都不改业务、不建 TEST、不改变占用。 |
| 幂等与防篡改 | 重复输入不重复写入，改动请求或回执后完整性检查失败。 |

## 剩余边界

- C07 目前整理和校验顾问输入，但不会自行联系外部顾问平台；后续由 C09/C12 接入实际调用入口。
- Boss 决定后任务仍保持原状态，安全恢复和占用释放归 C08。
- 本轮没有连接真实业务系统，也没有创建真实任务窗口或 TEST。
- 官方 `quick_validate.py` 已通过；C01–C13 全链回归和干净安装/回退验收均已通过。
