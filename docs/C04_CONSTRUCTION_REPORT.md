# C04 施工回执：任务包生成器

## 结果

状态：`DONE`（2026-08-12，已完成独立隔离验收）。C04 已实现为 `task-package-generator`，用于从已登记的 `PLANNED` Codex 任务生成待 Boss 审阅的私有任务包；不代表任务已派发、窗口已创建或业务施工已放行。

## 本次新增

- C04 `task-package-generator` Skill 与 UI 元数据。
- C04 brief、draft task package 与不可覆盖回执 schema。
- 私有 brief 验证、敏感值/绝对路径拒绝、源账本完整性与硬停门槛。
- 不派发任务包生成器、回执校验与 7 个隔离回归测试。

## 必须保留的边界

- 只从 C03 的 `PLANNED` 任务生成包；不改写 C03 任务状态。
- 包的状态固定为 `DRAFT_REQUIRES_BOSS_REVIEW`。
- 生成包后，派发、建窗、子 Agent、对象占用、TEST 和业务写入全部为 `false`。
- C05 占用检查仍是派发前置；C04 不冒充 C05。

## 独立隔离验收记录（2026-08-12）

验收在仓库外临时虚构项目中完成；没有创建真实任务窗口、子 Agent、对象占用、TEST 或业务对象。

| 验收点 | 实测结论 |
|---|---|
| 只读预演 | 只返回建议窗口与边界，`writePerformed=false`。 |
| 正式生成 | 只生成私有草案和不可覆盖回执；`dispatchAllowed`、建窗、TEST 和业务写入均为 `false`。 |
| C03 状态保护 | 生成后源任务仍是 `PLANNED`，C03 revision 未推进。 |
| 任务包完整性 | 草案回执与当前任务包校验通过。 |
| 自动回归 | C01–C04 共 21 个隔离测试通过。 |

## 剩余风险

- C05 已实现治理层占用与冲突检查，但不构成外部系统原生写前锁；C04 草案仍不能直接用于派发。
- 官方 Skill 校验器在本机缺少 YAML 依赖；本次使用无依赖预检与隔离回归测试，C13 仍须完成干净安装验证。
