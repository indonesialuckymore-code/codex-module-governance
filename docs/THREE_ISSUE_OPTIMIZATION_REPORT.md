# 三项运行问题优化回执

状态：`DONE`（产品规则与虚构隔离验收完成）。本文不代表任何真实业务项目已运行。

## 本次问题

1. 中央裁定讨论挤占中央上下文，容易把裁定窗口变成第二中央。
2. 中央或裁定窗口多次压缩后换新窗口，已有任务窗口可能找不到新中央。
3. 任务包名称、总账名称和 Codex 任务标题不一致，Boss 无法快速确认正在做哪项任务。

## 新增

- `CURRENT_CENTRAL`、`CURRENT_ADJUDICATION` 两个逻辑角色与代际路由。
- 计划换代交接包、激活回执、旧窗口只读边界。
- 仓库外私有任务事件箱，事件跨中央换代保留。
- 裁定完成后的中央影响摘要；完整讨论仍留裁定窗口。
- 任务身份合同：`任务ID｜业务名称`，运行窗口增加 `｜G代际`。

## 修改

- C03 登记唯一任务标题与窗口代际。
- C04 任务包显示名和任务身份与 C03 对齐。
- C07 同时回传原窗口完整决定与中央精简影响。
- C08 增加计划换代和持久事件续接。
- C10 在确认运行成功前核对任务 ID、完整标题、代际和项目归属。
- 中央、裁定、恢复、任务包和派发 Skill 更新为同一套运行规则。

## 复用与唯一归口

- 任务详细状态仍只在 C03，不新建第二总账。
- C07 仍复用 C03 `adjudications`，裁定窗口不持有任务状态权。
- C08 归口角色连续性和事件投递；C10 仍归口真实窗口派发。
- C06 + Boss 仍是唯一 `DONE` 路径，本次没有改变。

## 未做与边界

- 未创建、修改或验收任何真实业务项目、Base、workflow、服务或数据。
- 未把私有总账、裁定讨论、事件或交接包写入 GitHub。
- 聊天提醒仍可能延迟；可靠性来自私有事件箱，不是保证聊天必达。
- Codex 界面标题的真实回读仍依赖运行时接口；不匹配时只硬停，不自动改名猜测。

## Boss 验收

1. 用虚构项目建立中央 G1 和一个任务窗口 G1。
2. 第一次裁定生成裁定 G1；第二次裁定继续同一裁定角色，不再复制中央。
3. 查看中央影响摘要，确认没有顾问全文、Boss 理由和讨论过程。
4. 中央换为 G2，旧中央读取事件应被拒绝；新中央应看到换代前未确认事件。
5. 故意提交错误标题，系统应返回 `TASK_IDENTITY_MISMATCH / NEEDS_REVIEW` 且 C03 不变。

验证命令：`./scripts/verify-c10.sh`。它会递归运行 C03–C10 相关隔离回归。

最终证据：6 个受影响 Skill 均通过官方结构校验；C01–C13 共 168 项回归、插件合同、干净安装与回退 smoke 全部通过。
