# C05 施工回执：占用与冲突检查器

## 结果

状态：`DONE`（2026-08-12，已完成独立隔离验收）。C05 已实现为 `occupancy-conflict-checker`，用于在真实派发前检查对象占用、并行风险和新旧窗口选择；不代表窗口已创建或业务施工已放行。

## 本次新增

- `occupancy-conflict-checker` 固定 Skill 和界面元数据。
- C05 请求、决定与不可覆盖回执 schema。
- C04/C03 完整性校验、Boss 审阅与依赖门槛。
- 文件、表、字段、workflow、服务、配置、开关、事实源、账号和外部入口等对象检查。
- `objectKey` 精确冲突与 `conflictKey` 父子/共享资源冲突。
- 整包原子预留、后启动任务硬停、重复应用幂等和新旧窗口建议。

## 必须保留的边界

- C03 是唯一占用事实源；C05 不建立平行占用总账。
- 跨模块只接收 Fable 交接引用和结论，不读取或复制 Claude 细账。
- `ELIGIBLE` 不等于已派发；窗口、子 Agent、TEST 和业务写入均未发生。
- 当前是治理层锁，不冒充外部系统原生写前拦截。

## 独立隔离验收记录（2026-08-12）

验收在仓库外临时虚构项目中完成；没有连接真实任务、业务数据、TEST 或外部服务。

| 验收点 | 实测结论 |
|---|---|
| 安全整包 | 2 个虚构对象一次性预留，任务进入 `READY`；窗口和子 Agent 均为 0。 |
| 冲突整包 | 表/字段父子冲突被识别，后启动任务进入 `BLOCKED`；另一个无冲突对象没有留下部分占用。 |
| 新旧窗口 | 0 个旧窗口建议新开；唯一旧窗口建议沿用；不会实际建窗。 |
| 幂等与权限 | 重复应用不重复写账；非 `codex-module-central` 写入被拒绝。 |
| 完整性 | C05 决定绑定实际 C03 写入回执；决定、回执或关联凭证篡改均被发现。 |
| 自动回归 | C01–C05 共 33 个隔离测试通过，其中 C05 为 12 个。 |

## 剩余风险

- C05 仍是治理层占用，不是外部文件、Git、Base、workflow 或服务的原生写前锁。
- 当前不负责占用释放和失联恢复，须由 C08 接入；实际派发须等待 C09/C10。
- C06 已接续实现独立验收；`READY` 仍不能直接跳到 `DONE`，必须经过完成信号、中央验收和 Boss 最终批准。
- 官方 Skill 校验器在本机缺少 YAML 依赖；本次无依赖契约预检与隔离回归均通过，C13 仍须完成干净安装验证。
