# 安装说明（C01 阶段）

当前仓库只完成私有产品骨架，尚未包含可运行核心 Skill，因此不能安装后直接开展业务施工。

后续 C13 验收时，将提供：

1. 从私有 GitHub 仓库取得源码；
2. 注册本仓库 marketplace；
3. 安装 `codex-module-governance` 插件；
4. 新开 Codex 任务并执行首次初始化；
5. 在用户选择的仓库外私有目录建立项目数据。

在 C13 前，任何安装说明都只能用于开发预览，不能作为生产启用步骤。

