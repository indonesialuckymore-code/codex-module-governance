# C06 施工回执：回传与独立验收器

## 结果

状态：`DONE`（2026-08-12，已完成独立隔离验收）。C06 已实现为 `independent-handover-validator`，用于隔离任务窗口自报完成、中央独立验收和 Boss 最终批准；不代表已验收任何真实业务链路。

## 本次新增

- `independent-handover-validator` 固定 Skill 与界面元数据。
- 任务窗口回传、中央独立复核、验收决定和 Boss 最终决定 schema。
- 十类证据强制读回、范围/平行机制/未知写入/权限/错误/重复/风险检查。
- `NEEDS_REVIEW`、`PARTIAL`、`CONFLICT` 和 `PASS_PENDING_BOSS_APPROVAL` 结论。
- C06 独占的 `DONE` 路径、不可覆盖回执、关联 C03 回执校验与重复操作幂等。

## 必须保留的边界

- 施工窗口不能自行 `DONE`；中央通过也不能绕过 Boss 最终批准。
- 不读取 Claude 细账，不建立第二套任务状态。
- 不创建 TEST、不写业务对象、不释放 C05 占用。
- 当前验收程序提供证据合同和门禁；中央仍必须真正逐项读回原始证据。

## 独立隔离验收记录（2026-08-12）

验收在仓库外虚构项目中完成，没有连接真实任务、业务数据、TEST 或外部服务。

| 验收点 | 实测结论 |
|---|---|
| 完成信号 | 任务窗口完成信号只把任务推进到 `NEEDS_REVIEW`。 |
| 中央通过 | 十类虚构证据全部独立通过后，结果为 `PASS_PENDING_BOSS_APPROVAL`，任务仍为 `NEEDS_REVIEW`。 |
| Boss 最终批准 | 只有最终批准后，C06 才写入 `DONE`。 |
| 缺失/失败/冲突 | 未确认或缺证据保持 `NEEDS_REVIEW`；失败证据为 `PARTIAL`；未知写入方等为 `CONFLICT` 并硬停。 |
| 占用保护 | 任务 `DONE` 后，C05 原占用仍保留，等待 C08 释放。 |
| 完整性与幂等 | C06 决定和 Boss 最终决定均绑定实际 C03 回执；重复操作不重复推进，篡改被拒绝。 |
| 自动回归 | C01–C06 共 48 个隔离测试通过，其中 C06 为 15 个。 |

## 剩余风险

- C06 不能替代真正的业务证据读取；实际项目仍须中央访问原始快照、日志、记录历史和上下游对象。
- C08 已实现独立失联恢复与释放门；`DONE` 仍不会自动释放，必须重新验证本次 C06 最终批准并取得 Boss 独立释放授权。
- 官方 Skill 校验器在本机缺少 YAML 依赖；无依赖契约预检与隔离回归均通过，干净安装验证留到 C13。
