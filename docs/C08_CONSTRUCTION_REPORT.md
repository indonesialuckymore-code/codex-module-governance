# C08 施工回执：失联与恢复控制器

状态：`DONE`（2026-08-12，已完成独立隔离验收）。C08 已实现为 `disconnection-recovery-controller`；只验证虚构治理状态，不代表任何真实业务运行器已具备自动恢复。

## 本次新增

- C08 Skill、界面元数据、确定性恢复脚本和四份输入数据契约。
- 任务窗口、一级子 Agent 和中央失联冻结。
- C03 恢复冻结机械门：恢复期间拒绝所有非 C08 写入。
- Boss 授权的新中央接管、恢复决定和单调 `recoveryEpoch`。
- `DONE`/`CANCELLED` 两条独立占用释放路径及不可覆盖回执。
- `CURRENT_CENTRAL` / `CURRENT_ADJUDICATION` 逻辑角色、计划换代交接包、旧代际只读和持久任务事件箱。
- 18 项失联恢复回归 + 7 项角色连续性回归；C01–C13 全链共 168 项测试。

## 验收结论

| 验收点 | 结果 |
|---|---|
| 失联不丢现场 | 任务、窗口、子 Agent、占用、证据与裁定引用生成冻结快照。 |
| 冻结不再推进 | C03 普通任务、占用、完成、验收和裁定写入被机械拒绝。 |
| 接管不恢复施工 | 新中央经 Boss 授权和 epoch 校验后只恢复治理控制。 |
| 不重复施工 | 重复冻结、接管、决定和释放均幂等；损坏回执拒绝续读。 |
| 安全释放 | `DONE` 绑定 C06 最终批准；取消绑定回滚和零未完成写入，并再次要求 Boss 授权。 |
| 换中央不断线 | 换代前事件由新中央读取；旧中央无权读取或确认。 |
| 裁定不成第二中央 | 只允许一个当前裁定角色，不能写账、派工或验收。 |

## 剩余边界

- C08 不负责实际新建或复用任务窗口；恢复后的调度留给 C09/C10。
- C08 管理的是治理占用，不是外部系统原生锁；真实业务恢复仍须实时核查唯一写入源。
- 官方 Skill 结构校验、插件合同、Python 编译、168 项全链回归和 C13 干净安装/回退均通过。
